all:
  hosts:
    localhost:
        ansible_connection: local
  children:
    webservers:
      hosts:
        localhost:
            ansible_connection: local
    registries:
      hosts:
        localhost:
            ansible_connection: local
